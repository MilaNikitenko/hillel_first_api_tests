package entity;

/**
 * Record class representing a User.
 * @param email    the email of the user
 * @param password the password of the user
 */

public record User (String email, String password) { }
